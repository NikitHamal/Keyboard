<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data extraction policy.
  We explicitly disable cloud backup AND device-to-device transfer for every
  category. The keyboard is a text-handling component; its DataStore files can
  contain the user's clipboard history and learned word frequencies. Those
  must never leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </device-transfer>
</data-extraction-rules>
